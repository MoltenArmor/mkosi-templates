HTTP Boot netesp, with only `systemd-boot` inside.

This is a minimalist "chainloader" netesp. It does **NOT** contain a kernel or UKI, which means it relies on a external HTTP server to provide them.
